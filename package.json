{
  "name": "finhubkh-backtest",
  "private": true,
  "version": "1.0.0",
  "description": "XAUUSD chart terminal with local CSV data and strategy backtesting",
  "scripts": {
    "download:xauusd": "node scripts/download_xauusd.mjs",
    "resample:weekly": "python3 scripts/resample_weekly.py",
    "data": "npm run download:xauusd && npm run resample:weekly",
    "dev:api": "cd api && python3 -m uvicorn app.main:app --reload --port 8000",
    "dev:web": "cd web && npm run dev",
    "dev": "npm run dev:api",
    "install:all": "npm install && cd web && npm install && cd ../api && python3 -m pip install -r requirements.txt"
  },
  "dependencies": {
    "dukascopy-node": "^1.46.2"
  }
}
